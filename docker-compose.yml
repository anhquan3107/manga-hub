services:
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    restart: unless-stopped

  http:
    build: .
    command: api-server
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  tcp:
    build: .
    command: tcp-server
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "9090:9090"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  udp:
    build: .
    command: udp-server
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "9091:9091/udp"
    restart: unless-stopped

  grpc:
    build: .
    command: grpc-server
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "9092:9092"
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  redis-data:
